#include <stdio.h>

int main(){

   //read two numbers into variables a and b

   //add

   //display the result

   int a,b,c,d;
   scanf("%d %d %d",&a,&b,&d);
   c=a+b+d; //오른쪽을 계산해서 왼쪽에 넣어라 ('='->의미)
   printf("%d",c);

    return 0;
}

/*
file save: CTLR+S
terminal window: CTRL+~

file list: dir (windows), ls (mac)
change directory: cd
parent directory: ..  현재보다 위에 있는 parent directory
ex) cd ..
*/